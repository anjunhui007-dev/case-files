(()=>{function install(){const nav=document.querySelector('nav');if(!nav||nav.dataset.installed)return;nav.dataset.installed='1';nav.innerHTML='<button class="active" data-newtab="adventure"><strong>✦</strong><span>모험</span></button><button data-newtab="people"><strong>♙</strong><span>인물</span></button><button data-newtab="inventory"><strong>◇</strong><span>소지품</span></button><button data-newtab="equipment"><strong>♜</strong><span>장비</span></button>';nav.querySelectorAll('button').forEach(b=>b.onclick=()=>{nav.querySelectorAll('button').forEach(x=>x.classList.remove('active'));b.classList.add('active');const x=b.dataset.newtab;if(x==='adventure'){if(panel.open)panel.close();return}if(x==='people')showPeopleDirectory();if(x==='inventory')(window.showInventory||showInventory)();if(x==='equipment')(window.showEquipment||showStatus)()});window.showPeopleDirectory=showPeopleDirectory;window.openReader=openReader}function portrait(n){if(n.portrait)return `<img class="npc-list-photo" src="${esc(n.portrait)}" alt="">`;return `<div class="npc-list-photo npc-fallback">${esc(n.icon||'♙')}</div>`}function showPeopleDirectory(){const list=state.npcs||[];open(`<div class="people-directory"><header class="directory-head"><small>ENCOUNTERS</small><h2>만난 인물</h2><p>당신의 여정에서 이름과 얼굴을 알게 된 사람들.</p></header>${list.length?`<div class="people-list">${list.map(n=>`<button class="person-card" data-person="${esc(n.id)}">${portrait(n)}<span><b>${esc(n.name)}</b><small>${esc(n.role||'알게 된 인물')} · ${esc(n.relation||'아는 사이')}</small><em>${esc(n.note||'아직 자세히 알지 못한다.')}</em></span><i>›</i></button>`).join('')}</div>`:'<div class="empty-archive"><span>♙</span><h3>아직 만난 인물이 없습니다</h3><p>이야기에서 만난 인물은 여기에 자동으로 기록됩니다.</p></div>'}</div>`);pc.querySelectorAll('[data-person]').forEach(b=>b.onclick=()=>showPersonProfile(b.dataset.person))}function showPersonProfile(id){const n=(state.npcs||[]).find(x=>String(x.id)===String(id));if(!n)return;open(`<div class="npc-profile"><button class="archive-back" id="peopleBack">‹ 만난 인물</button><div class="npc-profile-hero">${portrait(n)}<div><small>ENCOUNTER PROFILE</small><h2>${esc(n.name)}</h2><p>${esc(n.role||'알게 된 인물')}</p></div></div><div class="profile-facts"><div><small>관계</small><b>${esc(n.relation||'아는 사이')}</b></div><div><small>친밀도</small><b>${Number(n.affinity||0)} / 100</b></div>${n.location?`<div><small>주요 장소</small><b>${esc(n.location)}</b></div>`:''}</div><section class="profile-note"><small>기록</small><p>${esc(n.note||'아직 자세히 알지 못한다.')}</p></section></div>`);$('#peopleBack').onclick=showPeopleDirectory}function openReader(title,body){document.body.classList.add('reading-mode');open(`<div class="reader"><header><small>ARCHIVE</small><h1>${esc(title)}</h1></header><div class="reader-body">${body}</div></div>`);panel.addEventListener('close',()=>document.body.classList.remove('reading-mode'),{once:true})}if(window.CHRONICLE_READY)install();else document.addEventListener('chronicle:ready',install,{once:true})})();