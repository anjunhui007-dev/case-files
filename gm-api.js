(()=>{const KEY='chronicle:openai-api-key';const MODELS={luna:'gpt-5.6-luna',terra:'gpt-5.6-terra',sol:'gpt-5.6-sol'};const DEFAULT_MODEL=MODELS.luna;const SYSTEM=`너는 판타지 TRPG CHRONICLE의 대화형 GM이다. 플레이어의 입력을 적극적으로 받아 즉시 세계가 반응하게 한다. 규칙 설명이나 시스템 관리보다 실제 장면, 인물의 말과 행동, 결과를 우선한다. 플레이어가 짧게 말해도 의도를 자연스럽게 이어 받아 진행하고, 불필요한 확인 질문·추천 선택지·번호 목록·'무엇을 할까요?' 같은 수동적인 마무리를 반복하지 않는다. 가능한 행동은 과감하게 판정하고 이야기를 앞으로 움직인다. 성공만 보장하지는 않지만 실패도 흥미로운 결과와 다음 상황으로 이어지게 한다. 전투·탐색·대화·거래·관계·기상천외한 자유 행동을 모두 TRPG GM처럼 유연하게 처리한다. 앱이 제공한 현재 캐릭터 상태와 실제 등록된 NPC·동료·소지품·장비·정수·Canon은 확정 사실이다. 과거 대화에 이름이 나왔더라도 현재 데이터에 등록되지 않은 인물을 기존 동료나 기존 지인으로 취급하지 않는다. 현재 데이터에 없는 인물을 이번 장면에서 새로 등장시키는 것은 가능하지만, 그 경우 반드시 처음 등장한 새 인물로 자연스럽게 설정한다. 존재하지 않는 과거 관계나 동료를 기억한다고 꾸며내지 않는다. 인물은 제공된 성격·관계·경험의 연속성을 유지한다. 세계는 아르켈리온 왕국이며 세계 유일의 12층 대던전은 30일마다 리셋된다. 플레이어에게는 오직 자연스러운 한국어 GM 서술만 출력한다. JSON, 태그, 상태변경 명령, DB, API, 내부 ID 같은 구현 정보를 출력하지 않는다. 일반적인 짧은 상호작용은 간결하고 빠르게, 발견·NPC·전투·중요 사건은 충분한 감각 묘사와 대사로 풍부하게 쓴다. 아이템이나 능력이 중요하면 이름·특징·효과·제약을 서술 속에서 명확히 확정해 후속 기록기가 이해할 수 있게 한다. 레벨업, 피해, 회복, 획득, 소비, 버림, 장비 변화, 동료 합류/이탈, 관계 변화, 이동, 시간 경과처럼 실제 상태가 변했다면 그 사실과 결과를 서술 속에서 명확하게 표현한다.`;function readKey(){try{return localStorage.getItem(KEY)||sessionStorage.getItem(KEY)||''}catch(e){return''}}function writeKey(v){try{v?localStorage.setItem(KEY,v):localStorage.removeItem(KEY)}catch(e){}try{v?sessionStorage.setItem(KEY,v):sessionStorage.removeItem(KEY)}catch(e){}}const getKey=()=>readKey();function setKey(v){v=String(v||'').trim();writeKey(v);sync();return!!getKey()}function ensure(){if(typeof state==='undefined'||!state)return;state.api??={};state.api.connected=!!getKey();state.api.mode='auto';state.api.models={...MODELS};state.api.recentTurns=Math.max(3,Math.min(8,Number(state.api.recentTurns)||6));state.gmBridge??={enabled:true,connected:!!getKey(),history:[]};state.gmBridge.enabled=true;state.gmBridge.connected=!!getKey();state.gmBridge.history=Array.isArray(state.gmBridge.history)?state.gmBridge.history:[];window.CHRONICLE_CONTENT?.ensure?.();window.CHRONICLE_COMMANDS?.ensure?.()}function sync(){ensure();if(typeof state!=='undefined'&&state)document.dispatchEvent(new CustomEvent('chronicle:statechange',{detail:{state}}))}function context(userText,opt){return window.CHRONICLE_CONTEXT?.build?.(userText,opt)||{action:userText}}function route(userText){const t=String(userText||'').toLowerCase();const nearby=(state?.npcs||[]).filter(n=>(state?.present||[]).includes(n.id));const activeQuests=Object.values(state?.contentRegistry?.quests||{}).filter(q=>!['완료','실패','completed','failed'].includes(q?.status));const solWords=['왕국','왕실','국왕','전쟁','멸망','세계','대륙','신','고대','운명','시간','차원','봉인','진실','배신','혁명','왕위','세계관'];const terraWords=['공격','전투','싸운','죽인','처치','도망','방어','스킬','퀘스트','의뢰','협박','설득','흥정','심문','보스','던전','비밀','중요','결투','매복'];let tier='luna',reason='일반 대화·탐색';const complexContext=nearby.length>=3||activeQuests.length>=4;if(solWords.some(w=>t.includes(w))&&complexContext){tier='sol';reason='복합 세계 변화'}else if(terraWords.some(w=>t.includes(w))||nearby.some(n=>n.important||n.major)){tier='terra';reason='전투·중요 상호작용'}if(solWords.filter(w=>t.includes(w)).length>=2){tier='sol';reason='복합 세계 변화'}return{tier,model:MODELS[tier],reason}}function textFromResponse(data){if(typeof data?.output_text==='string')return data.output_text;for(const out of data?.output||[])for(const c of out?.content||[])if(c?.type==='output_text'&&c.text)return c.text;return''}async function call(model,input,max_output_tokens=3200){const key=getKey();if(!key)throw new Error('API_KEY_MISSING');const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${key}`},body:JSON.stringify({model,input,max_output_tokens})});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data?.error?.message||`OpenAI API 오류 (${r.status})`);return data}async function request(userText){ensure();const routing=route(userText);state.api.lastRoute=routing;const ctx=context(userText);const data=await call(routing.model,[{role:'system',content:[{type:'input_text',text:SYSTEM}]},{role:'user',content:[{type:'input_text',text:JSON.stringify(ctx)}]}]);return{narration:String(textFromResponse(data)||'').trim(),_routing:routing,_context:ctx}}async function act(text){const response=await request(text);const geminiResults=await window.CHRONICLE_GOOGLE?.reconcileTurn?.({action:text,narration:response.narration,context:response._context})||{commands:[],artifacts:[],memories:[]};state.memories=Array.isArray(state.memories)?state.memories:[];for(const m of geminiResults.memories||[]){const entry=typeof m==='string'?{type:'gm',text:m}:m;if(entry?.text)state.memories.push({...entry,createdAt:entry.createdAt||Date.now()})}state.memories=state.memories.slice(-100);state.scene=Array.isArray(state.scene)?state.scene:[];state.scene.push({role:'user',text,time:Date.now()},{role:'gm',text:response.narration,time:Date.now()});state.scene=state.scene.slice(-80);state.choices=[];state.gmBridge.history.push({time:Date.now(),source:'openai+gemini',route:response._routing,action:text,commands:geminiResults.commands||[],artifacts:geminiResults.artifacts||[],results:geminiResults.results||[]});state.gmBridge.history=state.gmBridge.history.slice(-100);await save?.();return{narration:response.narration,_routing:response._routing,geminiResults}}async function test(){ensure();const results={};for(const [tier,model] of Object.entries(MODELS)){try{await call(model,'짧게 OK라고만 답해.',30);results[tier]={ok:true,model}}catch(e){results[tier]={ok:false,model,error:e.message}}}const failed=Object.values(results).filter(x=>!x.ok);if(failed.length)throw new Error(failed.map(x=>`${x.model}: ${x.error}`).join('\n'));return results}window.CHRONICLE_GM={getKey,setKey,ensure,request,act,test,route,context,MODELS,DEFAULT_MODEL};document.addEventListener('chronicle:ready',ensure);if(window.CHRONICLE_READY)ensure()})();