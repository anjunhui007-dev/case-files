(()=>{function ready(){return typeof state!=='undefined'&&state&&typeof open==='function'}function clean(v){return JSON.parse(JSON.stringify(v,(k,x)=>k==='portraitUrl'?undefined:x))}function exportJSON(){const payload={format:'chronicle-trpg-save',version:1,exportedAt:new Date().toISOString(),worldMode:'expanding-frontier',state:clean(state)};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`chronicle-save-day-${state.day}.json`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}let t=setInterval(()=>{if(!ready())return;clearInterval(t);const oldMenu=menu;menu=function(){oldMenu();const host=document.querySelector('#panelContent');if(!host)return;const card=document.createElement('div');card.className='card';card.innerHTML='<b>세이브 데이터</b><p class="muted">현재 캐릭터·세계·NPC·지도·기억 데이터를 JSON 파일로 내보냅니다. 프로필 이미지 파일은 제외됩니다.</p><div class="actions"><button id="exportJsonBtn">JSON 내보내기</button></div>';host.appendChild(card);document.querySelector('#exportJsonBtn').onclick=exportJSON};},50)})();